name: Primeiro Workflow de CI/CD
on:
  push:
    branches:
      - main  

jobs:
  explorar_pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Configurar Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Instalar Dependências 
        run: echo "Simulando a instalação de dependências..." 

      - name: Listar Arquivos do Projeto
        run: ls -la

      - name: Imprimir Mensagem de Sucesso
        run: echo "Prestou sim!"